# 🛒 Online Store
這是一個使用 **Flask + MongoDB** 製作的線上早餐店點餐系統，目前包含「商品列表、購物車、結帳、使用者登入」等功能。
### 1️⃣ 下載專案
```bash
建立.env
git clone https://github.com/Wang-3369/OnlineStore
cd OnlineStore
python -m venv venv
# Windows
venv\Scripts\activate
# macOS / Linux
source venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt

